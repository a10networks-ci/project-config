- project:
    name: a10-project-config
    node: bare-trusty

    jobs:
        - gate-{name}-linters

- project:
    name: sandbox
    github-org: openstack-dev
    vendor: a10networks

    jobs:
        - noop-check-communication
        - dsvm-tempest-full:
            node: devstack-trusty
