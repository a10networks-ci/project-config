- project:
    name: a10-project-config
    node: bare-trusty

    jobs:
        - gate-{name}-linters

- project:
    name: sandbox
    github-org: openstack-dev
    node: master
    vendor: a10networks

    jobs:
        - noop-check-communication:
            node: 'j-master || d-p-c'
        - dsvm-tempest-full:
            node: 'devstack-trusty || d-p-c'
