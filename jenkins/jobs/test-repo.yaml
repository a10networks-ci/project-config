
- job-template:
    name: test-repo
    node: bare-trusty

    builders:
      - shell: |
          #!/bin/bash -xe
          echo "Hello world, this is the {vendor} Testing System"

    triggers:
        - github
        - github-pull-requests
