
- job:
    name: batch-master-acos-client
    project-type: multijob
    node: master

    properties:
        - github:
            url: 'https://github.com/a10networks/acos-client'

    triggers:
        - github

    builders:
        - multijob:
            name: bare-phase
            condition: SUCCESSFUL
            projects:
                - name: acos-client-pep8-master
                    current-parameters: true
                    git-revision: true
                - name: acos-client-py26-master
                    current-parameters: true
                    git-revision: true
                - name: acos-client-py27-master
                    current-parameters: true
                    git-revision: true
                - name: acos-client-pypy-master
                    current-parameters: true
                    git-revision: true

- job:
    name: pulls-batch-acos-client
    project-type: multijob
    node: master

    properties:
        - github:
            url: 'https://github.com/a10networks/acos-client'

    triggers:
        - github-pull-requests

    builders:
        - multijob:
            name: base-phase
            condition: SUCCESSFUL
            projects:
                - name: acos-client-pep8-pulls
                - name: acos-client-py26-pulls
                - name: acos-client-py27-pulls
                - name: acos-client-pypy-pulls
